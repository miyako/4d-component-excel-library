//%attributes = {"shared":true}
//launch if not already launched, and activate.
//disable screen updating, suspend alerts.
//use this if a workbook should be hidden until its full content is complete. 

XCEL_util_EXECUTE("application_hide")